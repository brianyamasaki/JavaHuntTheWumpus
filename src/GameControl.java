/*
 * Game Control object: 
 * Handles events reported by the Graphical Interface, coordinates all the other parts of the game.
 */
public class GameControl {
	public GameControl() {
	    System.out.println("Starting GameControl");		
	}
	
	/*
	 * Example public method would be called when user moves to room X
	 * public moveToRoom(int x) {
	 * 	// send events to objects that need to know
	 * }
	 */
}